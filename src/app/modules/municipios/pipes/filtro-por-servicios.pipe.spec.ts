import { FiltroPorServiciosPipe } from './filtro-por-servicios.pipe';

describe('FiltroPorServiciosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPorServiciosPipe();
    expect(pipe).toBeTruthy();
  });
});
